<link rel="stylesheet" href="/css/user_profile.css">
<h1>
    Hello There/ Profile Page
</h1>


<% if(user.id && user.id == profile_user.id){ %>
    <!--if user matches then show the form-->
    <form action="/users/update/<%= profile_user.id %>" entype="multipart/form-data" method="POST">
        <input type="text" name="name" placeolder="Your Name" value="<%=p rofile_user.name %>" required>
        <input type="email" name="email" placeolder="Your Email" value="<%= profile_user.email %>" required>
        <br>
        <input type="file" name="avtar" placeholder="Profile Picture">
        <input type="submit" value="update">
    </form>
    <% }else { %>
        <!--else sow only the profile info-->

        <%= profile_user.name %>

            <% } %>